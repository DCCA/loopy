# dep-major-migration — lifecycle transcript

Migrating `left-pad` 1.0.0 → 2.0.0 (a major dep-updates deliberately skips).

1. advance → verify build **green** → **blocked** at gate `lp:approve`
2. human **approves** the bump
3. advance → **completed**, decision `approved` — manifest bump emitted
