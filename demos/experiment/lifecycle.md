# experiment — lifecycle transcript

Hypothesis: "Bigger CTA increases signups".

1. advance → **blocked** at gate `exp-cta:design` (design awaiting approval)
2. human **approves** the design
3. advance → launched (key `key-1`), **waiting** at `bake`
4. advance (results in) → readout written, **blocked** at gate `exp-cta:decision`
5. human **approves** the ship decision
6. advance → **completed**, final decision `ship`
