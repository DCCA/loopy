id: changelog

trigger:
  type: schedule
  cron: "0 6 * * 1" # weekly, Mondays 06:00 UTC

guardrails:
  pathAllowlist:
    - "CHANGELOG.md"
  maxFiles: 1
  skipIfOpenPr: true

config:
  changelogPath: "CHANGELOG.md"
